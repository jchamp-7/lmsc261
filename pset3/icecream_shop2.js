/*

    Create a const to declare the variable conesSoldPerHour, and assign an integer value.
    Use a counter variable hour, which will be incremented every hour.
    Output the value of cones sold each hour, and display the hour value. It should look like:

print(conesSoldPerHour * hour + " sold at hour " + hour);

Note the spaces before and after “ sold at hour “, which formats the output strings correctly.

Use a for loop that increments the counter variable from 1 through 12. Note that the program should count from hour 1 to 12 (not hour 0 to 11).

The first four outputs should look as follows:

3 sold at hour 1
6 sold at hour 2
9 sold at hour 3
12 sold at hour 4
*/

const conesSoldPerHour = 14;

for(let hour = 0; hour <= 12; hour ++);
print(conesSoldPerHour * hour + " sold at hour " + hour);